// version.js — the wire/gameplay version stamp.
// Bump when gameplay/wire format changes — stale tabs get told to refresh.
export const GAME_VERSION = 9; // v9: server-authoritative sim — the server runs the match, every browser renders
